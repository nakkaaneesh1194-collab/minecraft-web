import { NextRequest,NextResponse } from "next/server"; import { config,verify } from "@/lib/auth"; export const runtime="nodejs"; export async function GET(req:NextRequest){try{return NextResponse.json({session:verify(req.cookies.get("session")?.value,config().sessionSecret)??null});}catch(e){return NextResponse.json({session:null,configurationError:e instanceof Error?e.message:"Configuration error"},{status:503});}}
